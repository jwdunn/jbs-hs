<?xml version="1.0" encoding="utf-8"?>
<!-- This is the main layout(vertical view) it makes all the buttons, calls the color off the xml file, then calls the title off the String.xml.-->
<LinearLayout
		xmlns:android="http://schemas.android.com/apk/res/android"
		android:layout_width="fill_parent"
		android:padding="30dip"
		android:orientation="horizontal" android:layout_height="wrap_content">
		
	<LinearLayout
		android:layout_width="fill_parent"
		android:orientation="vertical" android:layout_height="wrap_content" android:layout_gravity="top">
		<TextView android:layout_marginBottom="25dip" android:layout_width="wrap_content" android:id="@+id/welcome_title" android:layout_height="match_parent" android:layout_gravity="center_horizontal" android:layout_weight="1" android:textSize="24.5sp" android:text="@string/dashboard_name"></TextView>
	<Button
		android:id="@+id/update_button"
		android:layout_width="fill_parent"
		android:text="@string/update_prompt" android:layout_height="wrap_content" android:layout_weight="1"/>
	</LinearLayout>
</LinearLayout>